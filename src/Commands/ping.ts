import { BaseCommand } from '@radon-types/command';

module.exports = {
    description: '',
    name: 'ping',
    options: {
        adminOnly: false // demo    
    },
    exec: (options: object) => {

    }
}